name = "housespecial-worker"
main = "src/index.ts"
compatibility_date = "2025-01-01"

[[d1_databases]]
binding = "DB"
database_name = "housespecial"
database_id = "d323071c-ff9b-4a7e-a08b-1f93692c918f"
migrations_dir = "migrations"

# Secrets — set via wrangler secret put, never commit:
# GOOGLE_CLIENT_ID
# GOOGLE_CLIENT_SECRET
# SESSION_SECRET      (run: openssl rand -hex 32)
# ALLOWED_ORIGIN      (https://housespecial.stephendove.com)
